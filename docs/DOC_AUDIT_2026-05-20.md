# Documentation / Comment Consistency Audit

**Date:** 2026-05-20
**Scope:** stale comments, docstrings, docs, TODO entries, and memory
records that contradict the current implementation after W1–W13, T1.1–T1.4,
and recent hole-closure work.
**Mode:** read-only — no source files were modified during this audit.

---

## How to read this report

- **Priority** — high / medium / low for the documentation-correctness risk
  (not the underlying engineering work).
- Findings are grouped by file, then ranked.
- After the tables: "safe docs-only fixes", "code-comment fixes", "things
  to delete", "things still accurate (do NOT change)", and "uncertain /
  needs confirmation".
- The recommended follow-up prompt at the bottom applies only the safe
  fixes.

---

## Ranked findings

| Pri | File | Loc | Stale / suspicious text | Why it's stale | Correct current behavior | Risk if left | Suggested action |
|---|---|---|---|---|---|---|---|
| **high** | `docs/SUMMARY.md` | line 14 | `main.py` "CORS hardcoded (TODO #11)" | #11 resolved 2026-05-19 (env-driven `CORS_ORIGINS`) | `_parse_cors_origins(os.getenv("CORS_ORIGINS"))` | New contributors look first at SUMMARY; misleads about whether deploy is gated | Drop the "(TODO #11)" parenthetical, say "CORS env-driven via `CORS_ORIGINS`". |
| **high** | `docs/SUMMARY.md` | line 42 | `notifications.py` "Marks `seen=true` before yield — TODO #4 bug." | #4a resolved 2026-05-19 (per-row mark-after-yield) | Mark-after-yield; `_yield_unseen` is the testable seam | Implies a known bug that's gone | Replace with "per-row mark-after-yield; LISTEN/NOTIFY deferred (TODO #4b)". |
| **high** | `docs/SUMMARY.md` | line 50 | `chat_service.py` "free-chat token matching — words only, no phrases TODO #5b" | #5b resolved 2026-05-19; `match_learning_words` now also matches phrases via `matcher_service` | Word + phrase polymorphic results | Misleads anyone refactoring chat matching | "matches words AND phrases (via matcher_service)". |
| **high** | `docs/SUMMARY.md` | line 51 | `guided_chat_service.py` "words only TODO #5b" | #5b resolved (all 3 priority tiers handle phrases) | Words + phrases at all tiers | Same | Drop "TODO #5b" and "words only". |
| **high** | `docs/SUMMARY.md` | line 60 | `recommendation_service.py` "`enrich_items` is word-only (TODO #5c)" | #5c resolved; `enrich_by_type` dispatches words + phrases + grammar | Multi-type dispatcher exists | Same | "use `enrich_by_type` for mixed types; `enrich_items` is the word backend." |
| **high** | `docs/SUMMARY.md` | line 62 | `usage_events_service.py` "Filter excludes `'transcript'` context (TODO #5a)" | #5a resolved 2026-05-18; `transcript` is in the IN clause | Includes `transcript` | Misleads insight-card work | Drop the parenthetical; note "now includes `transcript`". |
| **high** | `docs/SUMMARY.md` | line 63 | `matcher_service.py` "Module-level `os.chdir` import hack" | #3 resolved 2026-05-20 (path-based load) | `sys.path.insert(0, scraper_dir)` only | Misleads on import semantics | Drop the `os.chdir` line. |
| **high** | `docs/SUMMARY.md` | line 96 | `/review` → SRSReviewPage "(self-graded today TODO #0a)" | #0a-1 + #0a-2 resolved 2026-05-19; active = LLM-evaluated typed input | Active is real production, passive is reveal+self-grade with English prompt | Misleads about loop reality | Drop the parenthetical. |
| **high** | `docs/SUMMARY.md` | line 111 | "ResultCard.tsx — search result card" | Deleted in W8 (no importers) | File does not exist | Tells contributors to look at a non-existent file | Remove the bullet entirely. |
| **high** | `docs/SUMMARY.md` | line 113 | "WordStatusPicker.tsx … Dead-ends on unscraped words (Audit Hole 1)." | Hole 1 resolved 2026-05-20 (W2 / `Learn this anyway`) | Picker offers `Learn this anyway` when not in vocab | Misleads on user-loop completeness | Drop "Dead-ends …"; mention `Learn this anyway`. |
| **high** | `docs/SUMMARY.md` | line 126 | "SelectionReviewPanel.tsx … per-book; no global session UI yet TODO #5" | #5 resolved 2026-05-20: `ReadingReviewPage` exists with global session loop | Global session loop at `/reading-review` | Misleads on reading-review surface | "global ReadingReviewPage exists at `/reading-review`". |
| **high** | `docs/SUMMARY.md` | line 141 | useSearch "Hardcodes `language='de'` (Audit + TODO #10)" | #10 resolved 2026-05-19; hook takes `language` arg | Caller passes `recLanguage` | Same | Drop the parenthetical. |
| **high** | `docs/SUMMARY.md` | line 142 | usePreferences "Silent catch on failure (TODO #9)" | #9 resolved 2026-05-19; hook exposes `error`, keeps last-known prefs | Failures surface to UI; defaults aren't fabricated | Same | "exposes `error`; preserves last-known prefs on failure". |
| **high** | `docs/SUMMARY.md` | line 145 | useWordStatus "`recordTranscriptClick` fire-and-forget" | T1.1 resolved 2026-05-20; throws on non-2xx + dedup via sentence_id | Surfaces failures via `console.warn`, deduped at backend | Same | "errors surface via console.warn; dedup per sentence/day server-side". |
| **high** | `docs/SUMMARY.md` | line 200 | `subtitle-scraper/phrase_finder.py` "Currently has a bug (Doc vs str) breaking matcher tests" | #5b side-effect fix (2026-05-19); `match_sentence` now calls `_pf.nlp(sentence)` | All matcher tests pass | Implies broken state in a feature that works | Drop the bug note. |
| **high** | `docs/SUMMARY.md` | line 282 | "Where do free-chat words match against vocab? `chat_service.match_learning_words` (words only — TODO #5b)" | #5b resolved | Words + phrases | Same | Drop parenthetical. |
| **high** | `docs/SUMMARY.md` | line 285 | "Where do notifications get written? `subtitle-scraper/pipeline.py:_notify_user` (only on success — TODO #4)" | #4a resolved; `_mark_request` now emits `request_failed` too | Both success and failure notifications fire | Misleads operators reading notifications | Drop "only on success — TODO #4". |
| **high** | `docs/SUMMARY.md` | line 277 | "Where does a transcript click go? `routers/words.py:34` → `apply_progression('transcript_clicked')`" | Path still correct, but no mention of the dedup layer | Click hits `usage_events_service.record_transcript_click_event` first; progression only fires when insert was new | Misleads new contributors who think every click fires progression | Add "→ deduped first via `record_transcript_click_event`, then progression if newly inserted." |
| **high** | `docs/WORKFLOW_AUDIT.md` | lines 102–103 | Step 5 paragraph: "Card UI shows … German display text, a question ('Do you recognise and understand this?') … 'Show answer buttons' → 'I didn't know it' / 'I knew it ✓'." | Pre-T1.2 UI description; the prompt is now the English gloss, not the German display | Front = English gloss; reveal = German `answer_text`; question "Recall the German. Reveal, then self-grade." | Document contradicts the code | Rewrite per T1.2 (gloss-front, German-reveal). |
| **high** | `docs/WORKFLOW_AUDIT.md` | line 106 | Hole 13: "no dark mode … `background: '#fafafa'`, `border: '#e8eaf6'` … illegible in dark mode." | #20a/#20b shipped CSS-var theme tokens; SRSReviewPage uses `var(--color-*)` throughout | Dark mode works | Misrepresents UX state | Mark Hole 13 ✅ RESOLVED 2026-05-19. |
| **high** | `docs/WORKFLOW_AUDIT.md` | line 141 | Step 7: "Direction badge says 'Production', question changes to 'Can you use this naturally in a sentence?'. On reveal, an extra row shows `Target: <German word>`" | Pre-#0a-2; active is now typed input + LLM eval, prompt "Type the German for this item." | Active = text input + Submit + LLM evaluation | Same | Rewrite to reflect production-via-LLM. |
| **high** | `docs/WORKFLOW_AUDIT.md` | line 143 | Hole 21: "active review does not test production" | #0a-2 resolved 2026-05-19; `POST /srs/review/{card_id}/produce` + `evaluate_production` | Active is real production test | Loop status misrepresented | Mark ✅ RESOLVED 2026-05-19 (#0a-2). |
| **high** | `docs/WORKFLOW_AUDIT.md` | line 147 | Hole 22: "no active-direction display_text resolution" | #0a-1 added `prompt_text` / `answer_text` on payload | Payload carries both | Same | Mark ✅ RESOLVED 2026-05-19 (#0a-1). |
| **high** | `docs/WORKFLOW_AUDIT.md` | lines 240–256 | "## Summary — the loop that almost works" + "What doesn't, in priority order" | Lists 9 problems; most are now ✅ resolved (Hole 12, 21, 17, 18, 24, 25, 28, 30, 6, 7, 8, partial 11). Only Hole 23 (dual schedule accepted) and Hole 27 (spaced forgetting) remain | Most of the "doesn't work" list now works | Most damaging stale section — gives a wrong picture of project health | Rewrite the summary to reflect post-W7+T1 state; keep Hole 23 + Hole 27 as honest residuals. |
| **high** | `lexy-app/frontend/src/components/SRSReviewPage.tsx` | lines 559–562 | Comment: "Front of the card — prompt_text from the backend. **Passive cards: German display. Active cards: English gloss.**" | Backwards post-T1.2 — both directions now show **English gloss** on the front | Both passive and active use `prompt_text = gloss` | Misleads the next person editing review UI | Replace with "Both directions show the English gloss; passive reveals to German answer_text, active types German into the input." |
| **high** | `docs/TODO.md` | lines 760–764 | "#33 🟢 `pyfile` `pytest.ini` ignores legacy tests … `tests/legacy/*` glob-ignored" | `tests/legacy/` deleted and the `collect_ignore_glob` line removed from `conftest.py` in W8 | No `tests/legacy/` directory; no glob | Misleads anyone hunting for legacy coverage | Mark #33 ✅ RESOLVED (W8). |
| **high** | `docs/TODO.md` | line 345 | "Don't touch `tests/legacy/` (already `collect_ignore_glob`-skipped per root `conftest.py`)." | Same — both gone | None | Same | Drop the bullet. |
| **high** | `docs/TODO.md` | line 347 | #17 status: "Plan agreed … **Awaiting approval to begin batch 1 (the 20 tests above).**" | W4 shipped batch 1 (20 tests ported into `tests/runtime/`) | Batch 1 done; awaiting batch 2 | Misleads on what's open vs done | "Batch 1 landed 2026-05-20 in `tests/runtime/`; batch 2 not yet started." |
| **medium** | `docs/TESTS.md` | lines 47–57 | Frontend section: "one test file currently"; "Coverage is essentially zero on the frontend." | Vitest now at 179 across 31 files (per W13); many components/hooks covered | Frontend has substantial coverage | Misleads sizing for new test work | Replace count and list — point at the per-W rows that already document additions. |
| **medium** | `docs/TESTS.md` | line 53 | Existing-tests table lists only `TranscriptPanel.test.tsx` | 30+ frontend test files now exist | See W3/W4/W5/W7/W11/W13 rows | Same | Rebuild the existing-tests table or delete it and rely on the 🆕 history rows. |
| **medium** | `docs/TESTS.md` | line 63 | "Pipeline tests — `tests/` … ~900+ tests according to earlier exploration" | W4 row shows 562 passed (root); 900+ was pre-deletion of `tests/legacy/` | Root suite ≈ 562 passed (W4 baseline) | Misleads on scope of pipeline coverage | Update to "~562 passing (root); per `tests/runtime/` after W4 salvage batch 1". |
| **medium** | `docs/TESTS.md` | lines 79–101 | "Coverage gaps (backend) — none for books.py, content_requests, insights, notifications, recommendation_service.enrich_items for phrases" | Several gaps closed since (notifications, insights, content_requests, enrich_by_type) | Several gaps closed | Misleads on remaining gaps | Re-derive the list against the current `tests/` tree. |
| **medium** | `docs/TESTS.md` | lines 96–101 | "Frontend gaps: useWordStatus, useGuidedChat, useChat, useNotifications, WordStatusPicker (no test), SRSReviewPage (no behavioural test), API client error handling" | useWordStatus has a dedicated test file (W13/T1.1), WordStatusPicker has a test, SRSReviewPage has mobile + behavior tests, useNotifications has a test, `_http.test.ts` covers 401 handling | Several gaps closed | Same | Re-derive against the current frontend tree. |
| **medium** | `docs/CAPACITOR_READINESS.md` | lines 47–51 | "Still missing for Capacitor: No `capacitor.config.ts`, No `@capacitor/*` deps, No `ios/` directory" | ROADMAP T3.1 says packages installed, ios/ scaffolded, capacitor.config.ts written | Capacitor partially installed; only full Xcode-install + final cap sync pending | New readers think nothing's been done | Update three bullets to ✅ (packages installed, ios/ scaffolded with iOS 15 Podfile bump, capacitor.config.ts present with placeholder appId). |
| **medium** | `docs/CAPACITOR_READINESS.md` | lines 51–55 | "API base URL is path-relative — every `api/*.ts` uses `fetch('/api/v1/...')`. **Must be configurable** before wrap." | §8.1 says "Introduce VITE_API_BASE_URL — ✅ DONE 2026-05-20" — `apiUrl()` exists, all 14 api files migrated | API base URL is configurable | Self-contradicts §8.1 | Replace with "✅ `apiUrl()` helper; consumers route via `VITE_API_BASE_URL`." |
| **medium** | `docs/CAPACITOR_READINESS.md` | line 70 | "No App Store assets — privacy policy URL, account-deletion flow …" | W11 shipped `/privacy` + `DELETE /api/v1/account` + SettingsPanel destructive section | Privacy policy + deletion done; placeholder email remains | Misrepresents progress | "Privacy policy + account deletion ✅ (W11). Real privacy email still placeholder." |
| **medium** | `docs/CAPACITOR_READINESS.md` | §8.2 lines 439–444 | Bullets 3, 4, 6 "[ ] npm install", "[ ] npx cap init", "[ ] npx cap add ios" | All done per ROADMAP T3.1 | Done | Same | Tick the boxes. |
| **medium** | `docs/CAPACITOR_READINESS.md` | §8.4 lines 458–459 | Bullets 14, 15 "[ ] Privacy policy page", "[ ] Account-deletion endpoint + UI" | W11 shipped | Done | Same | Tick the boxes. |
| **medium** | `docs/TODO.md` | lines 806–816 | "Suggested execution order: 1. #0a #0b … 8. #3 …" | All 9 listed work items resolved | Most of order is already past-tense | Looks like a forward plan but is actually history | Rename to "Historical execution order" or move to a "What we shipped" appendix. |
| **medium** | `lexy-app/backend/services/usage_events_service.py` | line 7 docstring | "Valid contexts: 'free_chat' \| 'guided_chat' \| 'status_change' \| 'srs_review'" | Code also uses `'transcript'` (record_transcript_click_event + transcript context in routers/words.py legacy path) | Five contexts in use | Misleads people adding analytics filters | Add `'transcript'` (and audit reading flow for any extras); keep list authoritative. |
| **medium** | `lexy-app/backend/routers/reading.py` | line 139 docstring | "anchors — [{block_id, **token_index**, surface}]" | Schema uses `token_id` (migration 025); `_anchors_from_row` reads `a.get('token_id', 'legacy')` | Field is `token_id` (string) | Misleads anyone wiring a new client | Replace `token_index` with `token_id`. |
| **medium** | `lexy-app/backend/routers/reading.py` | lines 271–274 | `review_selection` docstring: "outcome='mastered' — move to 'mastered' status, exits review rotation" | Now also `apply_progression(status_marked_known, status_override='known')` when catalog match exists — promotes the catalog item to `known` (Hole 24 / #5) | Reading mastery propagates to main catalog | Hides cross-system effect | Add: "When a catalog item matches, also flips `user_word_knowledge.status='known'` via `status_marked_known` (Hole 24 fix)." |
| **medium** | `~/.claude/projects/-Users-emir-Documents-GitHub-language-app/memory/project_phase2_progression.md` | rule table | `status_marked_known +3/+1/correct/correct`; `status_marked_unknown 0/0/—/—`; `status_marked_learning +1/0/create/—`; "Deferred events" lists `transcript_clicked, free_chat_*, passive/active_review_*` as not yet implemented | All current rules differ: status_marked_known is `passive_srs=correct` only (#5d); status_marked_unknown is `passive_srs=incorrect, active_srs=incorrect` (#5d); status_marked_learning creates both passive AND active cards (#0b); all "deferred" events are now wired | See `progression_service._RULES` | Future Claude sessions will derive wrong invariants from this memory | Rewrite rule table; remove the "Deferred events" list. |
| **medium** | `~/.claude/.../memory/project_srs_review.md` | "Critical discovery: legacy srs_service.py is dead code" + tests-summary block | `srs_service.py` was deleted (TODO #1, 2026-05-19); rule-row summaries for review events are out of date; "Two-step Show answer buttons" describes passive flow only; active path now has typed input + LLM eval; test counts stale | See current `services/review_service.py`, `progression_service._RULES`, `SRSReviewPage.tsx` | Same | Replace the "dead code" warning with a one-liner noting srs_service is gone; refresh rule rows; describe the two-direction UI shapes. |
| **medium** | `~/.claude/.../memory/project_mobile_polish.md` | "Mobile polish is step 8 … must NOT be started until full end-to-end loop is confirmed working" | Mobile polish has shipped (#27a–g, #27h, #27i, T1.3). End-to-end loop is also done. | Mobile polish complete; iOS native wrap (T3.1) PAUSED per direction change | Future me will avoid touching mobile that's already shipped | Update to "Mobile polish shipped 2026-05-19/20 (#27a–i + T1.3); iOS Capacitor wrap PAUSED per ROADMAP T3.1." |
| **medium** | `~/.claude/.../memory/project_phase1_backend.md` | "services/word_service.py — get_user_knowledge, upsert_word_status"; "status validated by word_service.VALID_STATUSES set"; "What comes next: Frontend: auth flow, word click → mark known/learning, transcript reading mode, chat UI" | `upsert_word_status` + `VALID_STATUSES` deleted with #2 fix; "What comes next" all done | Status writes go via `apply_progression(status_override=...)`; status validated by Pydantic `Literal` | Misleads about word_service surface | Drop the dead-API references; mark "What comes next" as historic. |
| **medium** | `~/.claude/.../memory/project_ios_mobile_future.md` | Recommended sequence step 1 ("First land #27") and step 2 ("Then #20") + "current layout has hardcoded maxWidth: 900px, no media queries" | Both #27 and #20 done; PWA done; only Capacitor wrap remains | Mobile-responsive + theme system + PWA shell shipped | Memory presented as forward plan; mostly history | Trim to "Capacitor wrap is the remaining piece; ROADMAP T3.1 has the live state." |
| **medium** | `lexy-app/backend/services/word_service.py` | line 24 | "W3 fix (Hole 2): the **pre-T1.4 query** ended with `LIMIT 1`…" | Pre-fix shape was changed in W3 (Hole 2), not T1.4 (T1.4 was channel prefs / migration 027) | The fix is W3 | Cross-references the wrong work item | Replace "pre-T1.4" with "pre-W3". |
| **low** | `lexy-app/backend/services/playlist_service.py` | lines 32–37 | "Phrase support (deferred) — `_fetch_coverage` currently handles item_type='word' only." | Still accurate, but worth a re-check against current ROADMAP priorities | Still word-only | None | Leave or convert into a TODO ticket. |
| **low** | `lexy-app/backend/services/prioritization_service.py` | line 42 | "Future integration points (deferred)" | Needs a glance; phrase + grammar wiring largely landed elsewhere (#5b/#5c) but prioritization might still be word-centric | Likely still has some gaps | Low | Re-validate during next prioritization touch. |
| **low** | `lexy-app/backend/services/progression_service.py` | lines 44–47 | "Planned but deferred (no code path fires these yet): transcript_seen" | Accurate (`transcript_clicked` is wired; `transcript_seen` still deferred) | No change | None | Keep. |
| **low** | `lexy-app/backend/routers/notifications.py` | line 14 | "polling-based; LISTEN/NOTIFY is a planned follow-on (TODO #4b)" | Accurate; #4b deliberately deferred per ROADMAP T2.2 | No change | None | Keep. |

---

## 1. Safe docs-only fixes

A follow-up pass can apply these mechanically:

1. **SUMMARY.md** — strip every resolved `TODO #N` parenthetical and update the per-file one-liners to match current state (high-priority rows above).
2. **WORKFLOW_AUDIT.md** — mark Hole 13, Hole 21, Hole 22 ✅ RESOLVED; rewrite Step 5 + Step 7 paragraphs to the post-T1.2 / #0a-2 UI; rewrite the "Summary — the loop that almost works" section to reflect what's actually broken now (Hole 23, Hole 27, partial Hole 19/20).
3. **TODO.md** — mark #33 ✅ RESOLVED; drop the `tests/legacy/` reference inside #17; update #17 status to "Batch 1 shipped 2026-05-20 (W4); batch 2 not started"; rename "Suggested execution order" to "Historical execution order".
4. **CAPACITOR_READINESS.md** — flip "Still missing for Capacitor" bullets that ROADMAP T3.1 + §8.1 have already ticked; mark §8.2 bullets 3/4/6 and §8.4 bullets 14/15 as done; refresh §1 "API base URL is path-relative" to point at `apiUrl()`.
5. **TESTS.md** — replace the stale "one test file currently"/"essentially zero on the frontend"/"~900+ tests" lines with a fresh derivation (or delete the stale fixed-state sections and rely on the dated 🆕 rows that already document additions).

## 2. Code-comment / docstring fixes

6. **`SRSReviewPage.tsx:559–562`** — rewrite the back-of-front-comment so it reads "Both passive and active cards put the English gloss on the front (`prompt_text`); passive reveals German `answer_text` for self-grading, active expects German typed input."
7. **`services/usage_events_service.py:7`** — add `'transcript'` to the valid-contexts list (and audit `reading` if applicable).
8. **`routers/reading.py:139`** — change `token_index` → `token_id`.
9. **`routers/reading.py:271–274`** — add a sentence noting that `mastered` outcomes propagate to catalog `status='known'` via `status_marked_known`.
10. **`services/word_service.py:24`** — "pre-T1.4" → "pre-W3".

## 3. Things to delete

11. **`docs/SUMMARY.md` line 111** — drop the `ResultCard.tsx` bullet (file removed in W8).
12. **`docs/TODO.md` #33** — strip the body once marked resolved (or keep as a one-line history note).
13. **`docs/TODO.md` #17** — remove the legacy-tests bullet at line 345.
14. **`docs/CAPACITOR_READINESS.md`** — the "No `capacitor.config.ts`" / "No `ios/` directory" bullets that contradict §8.1 / ROADMAP T3.1.

## 4. Still accurate — do NOT change

- **`CLAUDE.md`** §1–§13. Heavily annotated with dates; the §8b "Loop invariants" section is current.
- **`docs/SCHEMA.md`** — accurate to migrations 023–028.
- **`docs/PRIVACY.md`** — correctly flags the email placeholder as the only outstanding pre-launch item.
- **`docs/ROADMAP.md`** — current; this is the live priority view.
- **`backend/services/progression_service.py`** module docstring, `_RULES` per-key comments, demotion docstring.
- **`backend/services/review_service.py`** docstrings.
- **`backend/services/llm_cache_service.py`** docstring (post-#24 thundering-herd guard).
- **`backend/services/settings_service.py`** (T1.3 + T1.4 reflected).
- **`backend/main.py`** lifespan comments.
- **`backend/routers/notifications.py`** docstring.
- **`backend/routers/words.py`** docstrings (transcript-click dedup behaviour documented).
- **`backend/services/rate_limiter.py`** module docstring + multi-process caveat.
- **`backend/services/playlist_service.py`** "deferred" notes — still accurate.
- **`progression_service.py`** "Planned but deferred: transcript_seen" — accurate.
- **Migration 026 / 027 / 028 headers** — all current.
- **`conftest.py`** — already cleaned in W8 (no `collect_ignore_glob`).
- **Memory `feedback_profiler.md`** + **`feedback_env_secrets.md`** — both still accurate; don't touch.
- **Memory `project_db_migration_todo.md`** — correctly says both items resolved.

## 5. Uncertain / needs confirmation

- **Memory `project_phrase_handling.md`**, **`project_insights_cards.md`**, **`project_grammar_support.md`**, **`project_book_reading_mode.md`**, **`project_interactive_reading.md`** — not deep-inspected in this pass. They likely mention behaviors that have shifted (Hole 17, 18, 24, 25, dual-schedule policy). Worth a one-pass sweep with the same lens.
- **`prioritization_service.py:42`** "Future integration points (deferred)" — needs a quick comparison against #5c (`enrich_by_type`) to confirm it's not partially landed.
- **`scripts/`** maintenance scripts — only spot-checked. The two new ones (`backfill_missing_active_srs.py`, `cleanup_orphan_srs_cards.py`) likely have current docstrings; older scripts might still reference deleted services.
- **`lexy-app/backend/services/matcher_service.py`** — quick grep showed no stale TODOs but the body wasn't read; the post-#3 path-based load needs a quick read.
- **`docs/TODO.md`** is enormous (~817 lines); the middle of the P1–P3 ✅-marked entries likely has more incidental staleness — comments like "(TODO #5b)" left inside resolved blurbs.

---

## Recommended second prompt (apply only the safe fixes)

```
Apply only the safe documentation-only fixes from
docs/DOC_AUDIT_2026-05-20.md (no code changes, no behavioural changes).

Scope (in this order):

1. docs/SUMMARY.md — strip every resolved-TODO parenthetical and refresh
   each one-liner so it matches current state. Specifically:
   - line 14:  drop "(TODO #11)"; mention CORS_ORIGINS env.
   - line 42:  replace "Marks seen=true before yield — TODO #4 bug" with
               "per-row mark-after-yield; LISTEN/NOTIFY deferred (TODO #4b)".
   - line 50:  drop "words only, no phrases TODO #5b".
   - line 51:  drop "words only TODO #5b".
   - line 60:  "enrich_items word-only (TODO #5c)" → "use enrich_by_type
               for mixed types; enrich_items is the word backend".
   - line 62:  drop "excludes 'transcript' (TODO #5a)"; note it now includes.
   - line 63:  drop the os.chdir mention.
   - line 96:  drop "self-graded today TODO #0a".
   - line 111: remove the ResultCard.tsx bullet (file deleted in W8).
   - line 113: drop "Dead-ends on unscraped words (Audit Hole 1)";
               note "Learn this anyway" affordance.
   - line 126: drop "(per-book; no global session UI yet TODO #5)";
               note global /reading-review page.
   - line 141: drop "Hardcodes 'language=de' (Audit + TODO #10)".
   - line 142: drop "Silent catch on failure (TODO #9)";
               note exposes error + preserves last-known prefs.
   - line 145: drop "fire-and-forget"; note errors surface via console.warn +
               dedup per sentence/day server-side.
   - line 200: drop the phrase_finder Doc-vs-str bug note.
   - line 277: add "→ deduped first via record_transcript_click_event,
               then progression only if newly inserted".
   - line 282: drop "(words only — TODO #5b)".
   - line 285: drop "(only on success — TODO #4)".

2. docs/WORKFLOW_AUDIT.md
   - Hole 13 (line 106): mark ✅ RESOLVED 2026-05-19 (#20a/b — CSS-var
     theme tokens; SRSReviewPage uses var(--color-*) tokens).
   - Hole 21 (line 143): mark ✅ RESOLVED 2026-05-19 (#0a-2 — active is
     real production via /srs/review/{id}/produce + LLM evaluation).
   - Hole 22 (line 147): mark ✅ RESOLVED 2026-05-19 (#0a-1 — payload
     carries prompt_text + answer_text).
   - Step 5 paragraph (lines 102–103): rewrite to the post-T1.2 UI
     (English gloss prompt; German on reveal; "Recall the German.
     Reveal, then self-grade.").
   - Step 7 paragraph (line 141): rewrite to the post-#0a-2 UI
     (typed input + Submit + LLM eval; "Type the German for this item.").
   - "Summary — the loop that almost works" (lines 240–256): rewrite
     to keep only the genuinely-still-open holes (Hole 23 accepted-not-
     closed dual schedule; Hole 27 spaced forgetting; Hole 19/20 free-
     chat language hardcoded — bundled into #19). Resolve the rest.

3. docs/TODO.md
   - #33 (lines 760–764): mark ✅ RESOLVED (W8 — tests/legacy deleted +
     collect_ignore_glob removed from conftest.py).
   - #17 (line 345 in description body): drop the
     "tests/legacy/* already collect_ignore_glob-skipped" bullet.
   - #17 status (line 347): change "Awaiting approval to begin batch 1"
     to "Batch 1 shipped 2026-05-20 (W4); batch 2 not started."
   - Section heading at line 806 "Suggested execution order":
     rename to "Historical execution order (mostly past-tense — kept
     for trace; current plan lives in ROADMAP.md)."

4. docs/CAPACITOR_READINESS.md
   - §1 "Still missing": flip three bullets to ✅ — capacitor.config.ts
     written, @capacitor/* installed, ios/ scaffolded (per ROADMAP T3.1).
   - §1 "API base URL is path-relative": replace with "✅ apiUrl()
     helper; consumers route via VITE_API_BASE_URL (§8.1 done 2026-05-20)."
   - §1 "No App Store assets — privacy policy URL, account-deletion":
     update to "Privacy policy + DELETE /account ✅ (W11); placeholder
     privacy email still open."
   - §8.2 bullets 3, 4, 6: tick the boxes.
   - §8.4 bullets 14, 15: tick the boxes (W11 / W12).

5. docs/TESTS.md
   - lines 47–57: replace "one test file currently" + "Coverage is
     essentially zero on the frontend" with the actual current count
     (~179+ tests across ~31 files per W13 row).
   - line 53: rebuild the "Existing tests" frontend table OR delete it
     and point at the dated 🆕 rows.
   - line 63: change "~900+ tests" to "~562 passed (root) per W4 baseline".
   - lines 79–101 and 96–101: re-derive backend + frontend coverage gaps
     against the current tests/ tree; drop entries that are now covered
     (notifications, content_requests, insights, useWordStatus,
     WordStatusPicker, SRSReviewPage, useNotifications, _http.ts).

6. Code-comment fixes (mechanical, no behaviour changes):
   - lexy-app/frontend/src/components/SRSReviewPage.tsx:559–562 —
     rewrite the back-of-card comment so it correctly says both
     directions put English gloss on the front; passive reveals German
     answer_text, active expects typed German input.
   - lexy-app/backend/services/usage_events_service.py:7 — add
     'transcript' to the valid-contexts list.
   - lexy-app/backend/routers/reading.py:139 — change "token_index"
     to "token_id" in the save_selection docstring.
   - lexy-app/backend/routers/reading.py:271–274 — extend the
     review_selection docstring to note that 'mastered' propagates to
     catalog status='known' via status_marked_known on catalog match.
   - lexy-app/backend/services/word_service.py:24 — change
     "pre-T1.4 query" to "pre-W3 query".

7. Memory files (~/.claude/projects/.../memory/):
   - project_phase2_progression.md: rewrite the rule-table block to
     match current progression_service._RULES (status_marked_learning
     creates both cards; status_marked_known is passive_srs=correct
     only; status_marked_unknown is passive+active 'incorrect'); remove
     the "Deferred events" list — everything is wired.
   - project_srs_review.md: remove the "Critical discovery: legacy
     srs_service.py is dead code" warning (file deleted); refresh
     rule rows; describe the two-direction UI (passive reveal+self-grade,
     active typed input + LLM eval).
   - project_mobile_polish.md: update to "Mobile polish shipped
     2026-05-19/20 (#27a–i + T1.3 dark-mode tristate); iOS Capacitor
     wrap PAUSED per ROADMAP T3.1."
   - project_phase1_backend.md: drop references to upsert_word_status +
     VALID_STATUSES (deleted in #2 fix); mark "What comes next" as
     historic.
   - project_ios_mobile_future.md: trim steps 1–3 from the sequence
     (mobile responsive, theme, PWA shell — all shipped); leave
     Capacitor wrap + APNs push as the remaining work.

Do NOT:
- Touch CLAUDE.md (it's current).
- Touch docs/SCHEMA.md, docs/PRIVACY.md, docs/ROADMAP.md (current).
- Touch any backend service code paths or rules.
- Re-derive frontend test counts beyond updating TESTS.md.
- Run formatters or tests.

After all edits, do a single `git status` + `git diff --stat` so the
user can sanity-check the scope.
```

**Estimated effort for the second prompt: medium / high.** Purely
mechanical, but ~50 edits across 8 files
(`SUMMARY.md`, `WORKFLOW_AUDIT.md`, `TODO.md`, `CAPACITOR_READINESS.md`,
`TESTS.md`, `SRSReviewPage.tsx`, `usage_events_service.py`, `reading.py`,
`word_service.py`) plus 5 memory files. Each edit is small (1–10 lines)
but the volume justifies high effort to keep the diffs focused and
avoid collateral changes. Should fit comfortably at `/effort high`;
`/effort max` is overkill.
